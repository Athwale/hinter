from wx._core import Size

main_window_size: Size = Size(800, 600)
default_border: int = 3

# These are very unlikely to ever appear in text with any meaning so we use those.
mark_italic: str = '†'
mark_bold: str = '‡'